/* eslint-disable @next/next/no-img-element */

/** Minh hoạ đăng nhập cho trang xác thực (ảnh tĩnh trong public/login.png). */
export function AuthIllustration() {
  return (
    <img
      src="/login.png"
      alt="Minh hoạ đăng nhập"
      className="relative z-[1] h-auto w-4/5 max-w-[460px]"
    />
  );
}
